# Location where the script works
export CACHE=$HOME/.installCache
# Location where logs and error logs are placed
export LOG=$CACHE/logs/log.txt
export ERROR_LOG=$CACHE/logs/error_log.txt
# Location where e.g. git repos are cloned/downloaded to
export DOWNLOADS=$CACHE/Downloads
# Lock file that exists, when the installation is running or hasnt been ended properly
export LOCK=$CACHE/installer.lock
